<h2>Add a Wall</h2>
<form method="POST" action="/walls">
  <label>Title:</label>
  <input type="text" name="wall[title]" value="<%= wall.title %>">
  <!-- The `[]`s in the "name" attribute here indicate that `params[:wall]` is
       going to be a hash with a key of "title" and a value of whatever the user
       puts in -->
  <br>

  <label>Description:</label>
  <textarea name="wall[description]"><%= wall.description %></textarea>
  <!-- We can add even more keys to the hash that will become `params[:wall]`;
       in this case, we now have params[:wall]["description"] available in our
       route -->
  <br>

  <label>Owner:</label>
  <input type="text" name="wall[created_by]" value="<%= wall.created_by %>">
  <br>

  <button>Create the Wall!</button>
</form>
